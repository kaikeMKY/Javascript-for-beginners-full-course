var charged = 0;
function charge(num) {
  return (num + 5) / 3;
}
charged = charge(10); // 10 + 5 / 3
console.log(charged);
var processed = 0;
function processedArg(num) {
  return (num +3 ) / 5;
}
processed = processedArg(7);
